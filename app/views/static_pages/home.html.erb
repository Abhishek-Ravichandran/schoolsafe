<% provide(:title, "Home") %>

<div id="map_part">
  <%= render partial: 'map', locals: { schools: @schools } %>
</div>

<div id="list">
  <%= render partial: 'filter' %>
  <div id="list_elems">
    <%= render partial: 'list', locals: { schools: @schools } %>
  </div>
</div>